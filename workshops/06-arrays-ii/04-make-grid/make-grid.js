// YOUR CODE BELOW
function makeGrid(num1, num2) {
  let num = [];
  let num3 = [];
  [];
  for (i = 0; i < num2; i++) {
    for (j = 0; j < num1; j++) {
      num.push(j + 1);
    }
    return num3.push(num);
  }
}
